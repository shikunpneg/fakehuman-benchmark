# 环境与密钥
.env
.env.*
!.env.example

# 运行时产物
__pycache__/
*.pyc

# 原始采集数据（可审计，但体积大；如需入库自行调整）
results/raw/
results/parsed/
results/*.jsonl
results/*.png
results/*.csv
# 子目录 figures 与 analysis 都不入库（默认产物，本地查看用）
results/analysis/

# 临时
_vendor/
